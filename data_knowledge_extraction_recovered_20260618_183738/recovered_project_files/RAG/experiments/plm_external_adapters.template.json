{
  "PURE": {
    "note": "Fill in the command for an external PURE implementation. The command must write summary_metrics.json to {output_dir}.",
    "command": "{python} path\\to\\pure_adapter.py --train {train_data} --dev {dev_data} --test {test_data} --output-dir {output_dir} --encoder {encoder} --max-length {max_length} --epochs {epochs} --batch-size {batch_size} --learning-rate {learning_rate} --device {device} --seed {seed}"
  },
  "TPLinker": {
    "note": "Fill in the command for an external TPLinker implementation. The command must write summary_metrics.json to {output_dir}.",
    "command": "{python} path\\to\\tplinker_adapter.py --train {train_data} --dev {dev_data} --test {test_data} --output-dir {output_dir} --encoder {encoder} --max-length {max_length} --epochs {epochs} --batch-size {batch_size} --learning-rate {learning_rate} --device {device} --seed {seed}"
  },
  "PRGC": {
    "note": "Fill in the command for an external PRGC implementation. The command must write summary_metrics.json to {output_dir}.",
    "command": "{python} path\\to\\prgc_adapter.py --train {train_data} --dev {dev_data} --test {test_data} --output-dir {output_dir} --encoder {encoder} --max-length {max_length} --epochs {epochs} --batch-size {batch_size} --learning-rate {learning_rate} --device {device} --seed {seed}"
  },
  "OneRel": {
    "note": "Optional OneRel adapter. The command must write summary_metrics.json to {output_dir}.",
    "command": "{python} path\\to\\onerel_adapter.py --train {train_data} --dev {dev_data} --test {test_data} --output-dir {output_dir} --encoder {encoder} --max-length {max_length} --epochs {epochs} --batch-size {batch_size} --learning-rate {learning_rate} --device {device} --seed {seed}"
  }
}
